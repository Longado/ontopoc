# 迭代 7：把极客夜话、ontexus、DIP 整合进我们的框架

2026-09-18 定。骨架不变：模型提议、代码核验、人来决定；每个用户动作最多三次模型调用；只在本机。

## 从哪来，对应补哪一块

| 来源 | 它有、我们缺的 | 这一轮怎么补 |
|---|---|---|
| 极客夜话 39 | 规则要分"描述了 / 检查了 / 强制了"，我们只做到描述；"用变更做压力测试"，客户发新版文件就对不上 | 阶段一、阶段二 |
| ontexus | Logic 层：规则从数据里发现，草稿 → 审核 → 发布 | 阶段一（只借做法，不借阈值） |
| DIP | 表单里中文名、描述、展示字段要人填；导入要逐对象的表和一张关系表 | 阶段三、阶段四 |

不做：ontexus 的 agent 质量审查、没有数据证据时让模型猜关系、RDF 以外的一串导出格式。理由见 `docs/DEVELOPMENT_GUARDRAILS.md` 和这次对 ontexus 规则的逐条对照。

## 阶段一：规则发现

- 做什么：代码在数据里找候选规则，人点"采纳 / 不要"。采纳的规则进数据体检，之后每次重跑都检查，违反的行列出来。
- 先做两种，都不需要任何阈值，都能被将来的数据违反：
  - 必填：这个对象的某个字段，每一个对象都有值。
  - 先后：同一个对象的两个日期字段，每一行都是前者不晚于后者。
- 取值范围、不为负两种先不做：我起草时各带了一个发明出来的特判（"每种值至少出现两次""不是全为 0"）。等前两种在真数据上跑过、数出规则条数和假规则，再决定要不要、怎么定义。
- 不做：阈值（"90% 都有值也算必填"）；强制执行（拦住写入）——我们不写数据。
- 改哪里：新文件 `rule_discovery.py`，`ontology_eval.py` 加一项体检，服务一个接口存采纳结果，页面在"数据体检"下加一个"规则"位置，MCP 加两个工具。
- 验收门：在芝加哥合同、Northwind、BART 上跑，列出每份数据找到几条、人工抽看有没有明显的假规则；把一行数据改坏，重跑后这条规则报违反。

## 阶段二：新版本文件

- 做什么：上传的文件名和某次运行相同、内容不同时，页面问一句"这是 X 的新版本吗"。是的话：上次的逐项确认预填、验收问题用新数据重算、采纳的规则重新检查；并给出"多了几个对象、少了几个、哪几条规则被违反"。
- 不做：自动认定是新版本（文件名一样不一定是同一份业务）；合并两版数据。
- 改哪里：运行记录加 `lineage`（第一版的运行名），确认、验收、规则按 lineage 存，旧的按文件哈希存的照样能读。页面上传框、服务保存、`ontology_confirm` 的预填入口。
- 验收门：拿芝加哥合同删 10 行、改 1 个编号当"新版本"上传，确认预填、验收问题重算、规则违反都出来，差异数和手算一致。

## 阶段三：页面上补 DIP 表单

- 做什么：对象的"属性"页里，中文名、描述、展示字段可以编辑。可选一次模型调用起草全部。
- 起草状态只记一个是否"模型写的、人还没改过"的标记，不另设机制。
- 新 Agent：字段释义员（第五个），按 `docs/AGENTS.md` 的四条进入条件：一次有边界的判断；代码核验每个字段真在、每个对象只有一个展示字段且在它的属性里；这个动作只有这一次模型调用；登记名字和定义卡。
- 改哪里：`agent_harness.AGENTS`、新提示词、`handover_form.py` 带上人填的列，服务一个保存接口，`ObjectPages.jsx` 属性页。
- 验收门：CMS 医院（英文表头）起草中文名，人改两处，保存后刷新还在；模型给了不存在的字段会被代码剔除并说明。

## 阶段四：导出给 DIP

- 做什么：一键下载，每个对象一张 CSV，列按 DIP 表单（主键、展示、中文名称、英文名称、描述、类型、长度、属性类型）。表单的列是可信的（`DIP_INTERFACE_REFERENCE.md` 第 0 节），导入契约不可信，所以文件里写明"导入前实测"。
- 不做：关系表（它的格式来自待实测的导入契约）；直接调 DIP 的导入接口；RDF 等格式。都等导入契约实测后再做。
- 改哪里：新文件 `dip_export.py`，服务一个下载接口，页面右上角加一个按钮，MCP 一个工具。
- 验收门：Northwind 导出，对象 CSV 的列和 `DIP_INTERFACE_REFERENCE.md` 第 2 节逐列对上，类型长度和属性页一致。

## 减法闸审过（2026-09-18）

采纳：规则先做两种；起草状态只留一个标记。没采纳：阶段二整段等触发、阶段四整段不做——这两段是作者明确要的"用变更做压力测试"和"对接 DIP"，护栏里"等触发"的条件由作者这次的要求代替；阶段四缩成只导出表单可信的那几列。另一条"属性表加类型长度两列"已在迭代 6 做了。

## 每一段照旧

先写失败测试并提交，再实现；在真公开数据上跑过；改了后端重启服务；页面按"少字、多图、分开放"；提交后推到 PR。

## 和结算线的关系

11-29 的结算线仍然是"一个不是作者的人用完一遍"。这四段都不代替试用，只让试用时多两样能给人看的东西：系统从你数据里找出的规则，和你发了新版文件后变了什么。

## 执行记录（2026-09-18 至 19）

**阶段一 规则发现。** 八份真数据上：先后规则 18 条全部说得通（Northwind 下单日期 ≤ 要求送达日期；芝加哥合同开始 ≤ 结束）；芝加哥执照的一条日期链原来报 15 条，去掉能由另外两条推出的之后剩 8 条，全是流程上的前后步。必填条数多（Northwind 30 个属性全有值），页面上按对象排成一行字段标签，可一键全部采纳。把芝加哥合同一行的结束日期改到开始之前，采纳的规则报 1 处违反并点出那份合同。取值范围、不为负没做。

**阶段二 新版本文件。** 芝加哥合同删 10 行、改 1 个合同号、改坏 1 行日期，按新版本上传：合同 3000 → 2990，新增 1、减少 11，和手算一致；供应商 931 → 930，手算一致；确认预填并马上对照；采纳的规则报出改坏的那份合同。撞出两个真问题，都已修：模型两次给同一个对象起了不同的 key（vendor / supplier），改成按"同一张表、同样识别字段"对齐；模型两次用了不同的识别字段（合同号 + 修订号 / 只用合同号），逐个对比会把所有合同算成删了又加，改成只报数量并写明"识别方式变了"。

**阶段三 DIP 表单。** 字段释义员（第五个 Agent）在 CMS 医院英文表头上起草：Facility ID → 医院编号，City/Town → 城市，ZIP Code → 邮政编码；展示字段选了 Facility Name。人改两处、保存、刷新后还在，其余 37 处保留"模型"标记。

**阶段四 导出。** Northwind 导出 5 个对象的 CSV，列和 DIP 表单一致，主键、类型、长度与属性页逐列相同；说明里点出订单明细靠订单号 + 产品号两个字段识别，DIP 只收一个主键，导入前要处理。

MCP 工具随之增加到 25 个（规则 2 个、表单 2 个、导出 1 个）。
