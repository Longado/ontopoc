# OntoPoc 交互问题记录

审查日期：2026-09-12。依据：本地隔离分支实际页面、DOM 操作和代码；截图在 `output/pm-audit-2026-09-12/`。不是客户试用结论。

| ID | 优先级 | 观察 / 用户影响 | 对应处理 | 状态 |
|---|---|---|---|---|
| UX-01 | P1 | 入口按合成/公开技术类型划分，FDE 与业务人员任务混合 | 默认进入“召回事件核对”，建模演示作为辅助入口；事件标题与范围先呈现 | 已实施并验证 |
| UX-02 | P0 | 材料 WIP-B17 与计算 work-order-wip-204 无映射，不能确认计算对象 | 合成材料、关系与引用对齐既有质量快照；验证结果事件和对象都在材料中，不编造别名映射 | 已实施并验证 |
| UX-03 | P0 | 可编辑文本框却仅接受预设，新材料输入后报错 | 固定演示材料明确只读，提供折叠/展开；不伪装任意材料建模 | 已实施并验证 |
| UX-04 | P1 | 只能逐条查询，不知道一个事件核对了多少、还剩多少 | 单事件清单，新增/粘贴多条、分类计数、选择条目查看详情、逐项失败反馈 | 已实施并验证 |
| UX-05 | P1 | 命中依据埋在整段原文，日期未核验不突出 | 在结果展示产品/UPC/批号/日期核验摘要，原文高亮输入批号与来源链接 | 已实施并验证 |
| UX-06 | P1 | “需复核”只留文字，不能补信息并重算 | 每行独立编辑与重算；保存复核原因、待补事项与经办人（本地记录，不外发），更改输入后结果和复核失效 | 已实施并验证 |
| UX-07 | P1 | 切换场景/刷新清空结果与复核 | 浏览器本地草稿、来源版本检查、恢复提示、保存失败提示及核对摘要下载 | 已实施并验证 |
| UX-08 | P1 | DeepSeek 实测是独立脚本，界面无新材料→候选→纠正→应用路径 | 本轮明确区分公告核对与 AI 提取；任意材料提取工作流留作独立后续任务，不能把既有实测成功当在线建模已交付 | 后续能力 |
| UX-09 | P2 | 生成后输入占半屏，结果密集；重复操作按钮缺少对象语境 | 结果生成后折叠材料，结果占主区域；每个按钮名称包含对象；保持移动端可用 | 已实施并验证 |

## 处理原则

保留确定性匹配、公开来源与“未匹配不代表安全”。清单中的查询行是用户待核对信息，不是公开数据库中真实库存；样例行明确为演示输入。人工复核不意味着执行召回、通知仓库或批准放行。

当前修改步骤见 [实施文档](superpowers/plans/2026-09-12-event-worklist-interaction.md)。各项完成后以测试/浏览器证据更新状态。

## 本轮验收结果

- 当前隔离分支：`codex/recall-scope-deepseek`。本地完成，尚未提交或发布。
- UX-01/04/06/07：浏览器添加 3 行练习输入，初始得到 matched / insufficient / conflict；第 2 行补 `X7549263` 后重新核对为 matched，第 1/3 行保持原结果。保存第 2 行的缺少标签日期补证事项后，切换场景、刷新，3 行结果与该复核均恢复。计数：2 匹配、1 冲突、1 需补证。
- UX-02/03/09：关系与实际质量快照逐项比对测试通过；浏览器确认材料只读、生成后收起、结果全宽，以及对象专属操作名称。
- UX-05/07：手机详情原文高亮 `X7549263`；实际下载 JSON 摘要，核验 3 行结果、复核理由、补证事项与边界。注入损坏 JSON 后刷新，页面进入恢复入口，原字符串保持不变。
- 318 项 Python 测试、101 项前端测试、4 项 Sites 路由/产物测试通过；构建、两份既有 artifact 检查、`git diff --check` 通过。Python 使用项目既有 3.13；系统 `python3` 版本较旧，不能作为本仓库回归环境。
- 桌面 1440×1000 与手机 390×844 截图已查看，页面宽度无横向溢出；浏览器未报告运行时错误。证据在 `output/interaction-verification-2026-09-12/`（本地忽略目录）。
- 本轮未修改模型提取逻辑，未新增 DeepSeek 调用。UX-08 尚未实现。Vite 仍提示主包超过 500 kB；性能拆包不在本轮交互修复范围。
